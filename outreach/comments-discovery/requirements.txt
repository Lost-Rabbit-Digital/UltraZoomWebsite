# Comments-discovery pipeline dependencies.
#
# `flask` is only needed for the local review_ui.py and is intentionally
# omitted here so CI runs stay lean — install it manually when reviewing.
anthropic>=0.40.0
beautifulsoup4>=4.12.0
requests>=2.31.0
