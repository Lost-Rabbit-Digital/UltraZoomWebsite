# Outreach pipeline dependencies. The pipeline is intentionally light on
# external libraries — most HTTP and JSON parsing uses stdlib.
#
# `google-auth` provides Application Default Credentials so the pipeline
# can authenticate to Google Sheets via Workload Identity Federation in
# CI (google-github-actions/auth@v2) and via `gcloud auth application-
# default login` locally. `requests` is the transport google-auth uses
# to refresh tokens.
google-auth>=2.27.0
requests>=2.31.0
